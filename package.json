{
  "name": "pan-africa-telecom",
  "version": "1.0.0",
  "private": true,
  "description": "Pan Africa Telecom monorepo: React/Vite frontend + NestJS API deployed as Vercel serverless functions.",
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.0",
    "@nestjs/core": "^10.3.0",
    "@nestjs/platform-express": "^10.3.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "express": "^4.21.2",
    "reflect-metadata": "^0.2.1",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.4.0"
  }
}
