# Copy this file to .env and fill in the values. .env is gitignored - never commit real credentials.

# Where the running frontend can be reached.
BASE_URL=http://localhost:5173

# Admin account used only to capture the Admin Dashboard screenshots.
ADMIN_EMAIL=
ADMIN_PASSWORD=
