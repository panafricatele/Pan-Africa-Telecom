{
  "name": "pat-pdf-generators",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate:scope": "node generate-scope-pdf.mjs",
    "generate:sections": "node generate-website-pdf.mjs",
    "generate:quotation": "node generate-quotation-pdf.mjs",
    "generate:all": "npm run generate:scope && npm run generate:sections && npm run generate:quotation"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  }
}
